import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles(() => ({
  root: {
    height: '100vh',
  },
  login: {
    background: '#333',
    height: '100vh',
  },
}));
